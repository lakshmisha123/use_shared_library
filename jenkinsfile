@Library('shared-lib') _

pipeline {
    agent any

    stages {
        stage('Call shared library') {
            steps {
                script {
                    gitCheckout()
                }
            }
        }
    }
}
