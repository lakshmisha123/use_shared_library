@Library('jenkins_shared') _

pipeline {
    agent any

    stages {
        stage('Call shared library') {
            steps {
                script {
                    gitCheckout()
                }
            }
        }
    }
}
