@Library('shared-lib') _

pipeline {
    agent any

    stages {
        stage('shared') {
            steps {
                script {
                    gitCheckout()
                }
            }
        }
    }
}
